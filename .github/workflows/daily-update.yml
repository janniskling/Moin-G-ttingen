name: Daily Updates

on:
  schedule:
    # Run at 04:00 UTC every day
    - cron: '0 4 * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  scrape-and-update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Scrape Events
        run: node scripts/scrape-events.cjs
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}

      - name: Scrape Deals
        run: node scripts/scrape-deals.cjs
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}

      - name: Commit and Push if changed
        run: |
          git config --global user.name 'Göttingen Guide Bot'
          git config --global user.email 'bot@noreply.github.com'
          git add public/events-data.json public/deals-data.json
          git commit -m "update: daily data (events & deals) [skip ci]" || exit 0
          git push
